# SZK-46: Daily Severity Trend
# The day-level view of SZK-09. Best chart for a report: it shows the
# whole engagement window and the day severity escalated.
uuid: 5a46c3d4-e5f6-7890-abcd-ef0123450046
version: 1.0.0
dataset_uuid: 7c1f9d2a-6b35-4e88-9a10-0d5f3b7e4c21
slice_name: Daily Severity Trend
viz_type: echarts_timeseries_bar
description: >
  Daily detection volume stacked by severity (SZK-46). The report-ready
  companion to the hourly SZK-09: it fits a long engagement window on one
  screen and makes the day severity escalated obvious.
query_context: ""
params:
  x_axis: detected_at
  time_grain_sqla: P1D
  granularity_sqla: detected_at
  time_range: No filter
  metrics:
    - expressionType: SQL
      sqlExpression: COUNT(*)
      label: detections
      hasCustomLabel: true
      optionName: metric_szk46_detections
  groupby:
    - level
  adhoc_filters: []
  row_limit: 10000
  stack: true
  x_axis_title: Date (UTC)
  y_axis_title: Detections
  color_scheme: supersetColors
  rich_tooltip: true
  zoomable: true
cache_timeout: null

# SQL used to power this chart:
# SELECT DATE_TRUNC('day', detected_at) AS day_bucket, level, COUNT(*)
# FROM suzaku_detections GROUP BY 1, 2 ORDER BY 1
