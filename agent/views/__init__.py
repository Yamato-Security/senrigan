"""Streamlit page modules rendered by the ``st.navigation`` app in ``app.py``.

Pages own layout only. Page chrome (``st.set_page_config``) belongs to the entry
point, and every piece of logic they use lives in a testable module
(``profiles.py``, ``handlers.py``, ``query.py``, ``suzaku_db.py``).
"""
