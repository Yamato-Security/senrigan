# 変更点

## 1.0.0 [2026/07/31] - Black Hat Arsenal USA 2026 Release

**新機能:**

- Material for MkDocsによる多言語対応のドキュメントサイトを追加し、GitHub Pagesで公開した。あわせてREADMEを、そのサイトへ誘導するランディングページに置き換えた。 (#38 #39) (@YamatoSecurity)
- Suzakuとの連携機能を追加した。`aws-ct-summary`およびタイムラインのDuckDBビュー、Suzakuの検知結果を表示するダッシュボード、サマリとメトリクスを閲覧するエージェントのエクスプローラーページを追加し、SuzakuのSigma検知結果を同一のデータベース上で生のCloudTrailイベントと並べて分析できるようにした。 (#32 #70 #71 #72 #73 #75) (@fukusuket)
- 取り込んだ期間内で出現回数が最も少ないAPIコール・プリンシパル・送信元IPを表示する「Rare Events」ダッシュボードを追加した。件数順のチャートとは逆の視点であり、1回だけの異常なコールは件数順では埋もれてしまう。 (#42) (@fukusuket)
- Threat Technique Catalogのマッピングを追加し、ハントおよびダッシュボードのチャートがMITRE ATT&CKのテクニック情報を持つようにした。 (#45) (@fukusuket)
- クエリ結果のIPカラムに対するGeoIPの自動付与を追加した。これにより、送信元IPを返すハントやアドホッククエリでは、手動での結合なしにASN・都市・国が得られる。 (#43) (@fukusuket)
- サマリの解析処理とアップローダーが、既存の形式に加えてJSON Lines形式にも対応した。 (#35) (@fukusuket)
- AWS Configスナップショットのパスを対象としたリポジトリ全体の整合性テストを追加した。これにより、アセットの移動や名前変更があった場合、実行時にダッシュボードが静かに壊れるのではなくCIで検出される。 (#68) (@fukusuket)
- Dockerのクリーンアップ用に`clean`コマンドをMakefileに追加した。 (#30) (@fukusuket)

**改善:**

- `SUPERSET_SECRET_KEY`をデフォルト値の同梱ではなく自動生成するようにした。これにより、新規に構築した環境がリポジトリ上で公開されている鍵で署名されることがなくなる。 (#63) (@fukusuket)
- コンソールログインイベントの追跡を強化し、コンソールログインおよびMFAのトレンドチャートのSQL式を修正した。 (#23 #40) (@fukusuket)
- 取り込みの各フェーズとGeoIP検索の性能ログを追加した。これにより、取り込みが遅い場合に推測ではなくフェーズ単位で原因を切り分けられる。 (#24) (@fukusuket)
- エージェントのモジュールから、セッション状態の管理とチャート描画をそれぞれ別モジュールに切り出す形でリファクタリングした。 (#78 #79) (@fukusuket)
- CloudTrailの追加のアクティビティを対象としたダッシュボードのチャートを追加した。 (#34) (@fukusuket)
- プロジェクトのロゴと、READMEおよびドキュメントのランディングページにDEF CON 2026のバッジを追加した。 (#28 #64) (@fukusuket) (@YamatoSecurity)

**バグ修正:**

- AIエージェントが、アナリストの入力に由来するSQLを読み書き可能なDuckDB接続で実行しており、かつガードを回避できる余地があったため、細工されたプロンプトによって想定外の変更や読み取りを行う文に到達し得た。読み取り用接続をサンドボックス化し、SQLのガードを強化した。 (#46 #52) (@YamatoSecurity)
- エージェントが、設定されたLLMエンドポイントとの通信時にTLS証明書の検証を無効化していたため、通信を傍受される可能性があった。設定されたCAバンドルを使用して検証するようにした。 (#47 #53) (@YamatoSecurity)
- AIによる分析結果とアナリストのメモがエスケープされないままHTMLレポートに埋め込まれていたため、ログ由来の内容によってブラウザで開いたレポートにマークアップを注入できた。いずれもエスケープするようにし、分析用プロンプトも強化した。 (#48 #54) (@YamatoSecurity)
- Supersetが安全でないデフォルト設定（公開されているシークレットキー、CSRF保護の無効化、読み書き可能なマウント）のまま同梱されていた。3点とも解消した。 (#51 #56) (@YamatoSecurity)
- 取り込み済みのファイルが変更されていた場合、`ingested_files`への記録がUPSERTではなくINSERTで衝突するため、`ingester`が処理全体を中断していた。UPSERTするようにし、変更されたログの再取り込みが継続できるようにした。 (#49 #57) (@YamatoSecurity)

**その他:**

- すべてのGitHub ActionsをコミットSHAで固定し、Dependabotを導入した。これにより、侵害されたアクションやタグの付け替えがビルドに紛れ込むことを防げる。 (#50 #55) (@YamatoSecurity)
- Dockerのベースイメージを更新し、CIおよびリリースワークフローをNode.js 24に移行し、`actions/checkout`と`actions/upload-artifact`を更新した。 (#26 #36 #37 #76) (@fukusuket)
- npmおよびpipのレジストリURLを更新し、pipのインストールに`--uploaded-prior-to P30D`を指定した。これにより、直近30日以内に公開されたパッケージがイメージのビルドに入り込むことを防げる。 (#25 #27) (@fukusuket)
- `make`コマンドを更新した。 (#69) (@fukusuket)

## 0.1.0 [2026/06/12]

- 最初の公開リリース。CloudTrailのingester、Supersetダッシュボード、AIハンティングエージェント、AWS Configのリソースグラフを含む。
